<!-- draft: tobe-adopt · 근거: 요구사항·확정 문답 9건·예약 스펙 3건·영향 스펙 없음 -->
# SR-234 변경명세

> 기계 채택(zero-LLM): 요구사항 요지·확정 문답·예약 스펙을 **그대로** 옮겼다. 지어낸 사실은 없다 — `[미상]`은 사람이 채운다.
> 근거 파일: `docs/변경관리/SR-234/00_요구사항.md` · 확정 문답 9건

## AS-IS → TO-BE 비교
### SR-234 (요구사항 요지 — 전 스펙 공통)
- AS-IS: 없음 — 이 요구는 현행에 없는 기능이다
- TO-BE (근거: 요구사항·확정 문답):
  - [유형: 화면+API] 이메일/휴대폰 인증 후 새 비밀번호 설정. 링크·코드는 10분 유효, 1회용.
  - UI/UX: 현재 단계·남은 유효시간 표시 · 재전송은 60초 쿨다운.
  - 수용 기준: 만료 링크는 '만료됨 + 다시 요청' · 재설정 후 모든 기기 로그아웃
  - 이번 SR에서 하는 것과 명시적으로 빼는 것은 무엇인가? → 포함: [유형: 화면+API] 이메일/휴대폰 인증 후 새 비밀번호 설정. 링크·코드는 10분 유효, 1회용. UI/UX: 현재 단계·남은 유효시간 표시 · 재전송은 60초 쿨다운. 수용 기준: 만료 링크는 '만료됨 + 다시 요청' · 재설정 후 모든 기기 로그아웃 / 제외: 요구 본문에 적히지 않은 것 전부
  - 반드시 유지돼야 하는 기존 동작(회귀 범위)은 무엇인가? → 기존 로그인(SR-232)·가입(SR-231) 흐름과 MEMBER_LOGIN_ATTEMPTS·리프레시 토큰 동작은 그대로. 재설정 완료 시 그 회원의 리프레시 토큰 전부 폐기(모든 기기 로그아웃)
  - 기존 클라이언트와의 하위호환이 필요한가? → 기존 필드명·타입·의미는 그대로 둔다(추가만)
  - 오류 응답 계약(코드·메시지)은 무엇인가? → 기존 오류 계약(코드 MBR-4xxx + message) 그대로. 만료 링크/코드는 410 MBR-4101 '만료됨 — 다시 요청', 존재 여부는 새지 않게 요청 API는 항상 202
  - 타 도메인 테이블·집계(정산·통계류)에 파급이 있는가? → MEMBER_PASSWORD_RESETS 테이블 신설(대상 email/phone_norm · 코드 해시 · 만료 시각 · 소비 시각 · 시도 횟수 · 생성 시각) + MEMBERS.password_hash 갱신·updated_at. 기존 컬럼 의미 변경 없음
  - 기존 데이터 이관·백필이 필요한가? → Flyway V4__member_password_resets.sql — CREATE TABLE IF NOT EXISTS(랩 규칙 ddl-idempotent). 기존 행 이행 없음
  - 변경 대상 화면이 전부 나열됐는가? (누락 화면 없음 확인) → shop-web에 비밀번호 재설정 화면 1개(요청→코드 확인→새 비밀번호 3단계, 현재 단계·남은 유효시간·재전송 60초 쿨다운 표시). 로그인 화면에 '비밀번호를 잊으셨나요' 링크만 추가
  - 빈 값·오류 상태의 화면 표기는 무엇인가? → 0건·오류·로딩 표시는 기존 규칙(UIS §5)을 그대로 따른다
  - 이 변경으로 생기거나 바뀌는 화면 상태 중 스토리(상태별 실물)로 남길 것은 무엇인가? → 바뀌거나 새로 생기는 상태마다 스토리를 남긴다(기존 스토리는 깨뜨리지 않는다)

### UIS-MBR-003 (비밀번호 재설정 화면 · 신규 UIS · 구현 모듈 shop-web)
- AS-IS: 없음 — 신규 스펙(예약 FUNC-member-007)
- TO-BE: 비밀번호 재설정 화면 — 위 SR-234 절의 요지·문답을 계약으로 신규 구현 (모듈 `shop-web` 안에)

### INF-MBR-006 (비밀번호 재설정 코드 요청 API · 신규 INF · 구현 모듈 shop-api)
- AS-IS: 없음 — 신규 스펙(예약 FUNC-member-008)
- TO-BE: 비밀번호 재설정 코드 요청 API — 위 SR-234 절의 요지·문답을 계약으로 신규 구현 (모듈 `shop-api` 안에)

### INF-MBR-007 (비밀번호 재설정 확정 API · 신규 INF · 구현 모듈 shop-api)
- AS-IS: 없음 — 신규 스펙(예약 FUNC-member-009)
- TO-BE: 비밀번호 재설정 확정 API — 위 SR-234 절의 요지·문답을 계약으로 신규 구현 (모듈 `shop-api` 안에)

## 제외(변경 없음 명시)
- (없음 — 영향분석에서 고른 스펙만 위에 있다)

## 회귀 범위
- 기존 로그인(SR-232)·가입(SR-231) 흐름과 MEMBER_LOGIN_ATTEMPTS·리프레시 토큰 동작은 그대로. 재설정 완료 시 그 회원의 리프레시 토큰 전부 폐기(모든 기기 로그아웃)
- 기존 필드명·타입·의미는 그대로 둔다(추가만)
- 기존 오류 계약(코드 MBR-4xxx + message) 그대로. 만료 링크/코드는 410 MBR-4101 '만료됨 — 다시 요청', 존재 여부는 새지 않게 요청 API는 항상 202
- MEMBER_PASSWORD_RESETS 테이블 신설(대상 email/phone_norm · 코드 해시 · 만료 시각 · 소비 시각 · 시도 횟수 · 생성 시각) + MEMBERS.password_hash 갱신·updated_at. 기존 컬럼 의미 변경 없음
- Flyway V4__member_password_resets.sql — CREATE TABLE IF NOT EXISTS(랩 규칙 ddl-idempotent). 기존 행 이행 없음
- 0건·오류·로딩 표시는 기존 규칙(UIS §5)을 그대로 따른다
- 바뀌거나 새로 생기는 상태마다 스토리를 남긴다(기존 스토리는 깨뜨리지 않는다)

## 롤백 요지
- 신규 스펙(UIS-MBR-003, INF-MBR-006, INF-MBR-007)의 코드·테이블을 역순으로 제거한다. 기존 스펙 변경 없음
