<!-- draft: tobe-adopt · 근거: 요구사항·확정 문답 9건·예약 스펙 3건·영향 스펙 없음 -->
# SR-232 변경명세

> 기계 채택(zero-LLM): 요구사항 요지·확정 문답·예약 스펙을 **그대로** 옮겼다. 지어낸 사실은 없다 — `[미상]`은 사람이 채운다.
> 근거 파일: `docs/변경관리/SR-232/00_요구사항.md` · 확정 문답 9건

## AS-IS → TO-BE 비교
### SR-232 (요구사항 요지 — 전 스펙 공통)
- AS-IS: 없음 — 이 요구는 현행에 없는 기능이다
- TO-BE (근거: 요구사항·확정 문답):
  - [유형: 화면+API] 이메일/비밀번호 로그인, 30일 자동 로그인(리프레시 토큰), 5회 실패 시 10분 잠금.
  - UI/UX: 실패 사유는 구체적으로(비밀번호 오류 n/5) · 비밀번호 보기 토글 · 로그인 후 원래 가려던 페이지로 복귀.
  - 수용 기준: 잠금 상태는 API 429 + 남은 시간 · 로그아웃 시 리프레시 토큰 폐기 · 세션 만료 시 장바구니 유지
  - 이번 SR에서 하는 것과 명시적으로 빼는 것은 무엇인가? → 포함: [유형: 화면+API] 이메일/비밀번호 로그인, 30일 자동 로그인(리프레시 토큰), 5회 실패 시 10분 잠금. UI/UX: 실패 사유는 구체적으로(비밀번호 오류 n/5) · 비밀번호 보기 토글 · 로그인 후 원래 가려던 페이지로 복귀. 수용 기준: 잠금 상태는 API 429 + 남은 시간 · 로그아웃 시 리프레시 토큰 폐기 · 세션 만료 시 장바구니 유지 / 제외: 요구 본문에 적히지 않은 것 전부
  - 반드시 유지돼야 하는 기존 동작(회귀 범위)은 무엇인가? → 기존 동작(조회·정렬·표시 규칙)은 그대로 유지한다
  - 기존 클라이언트와의 하위호환이 필요한가? → 기존 필드명·타입·의미는 그대로 둔다(추가만)
  - 오류 응답 계약(코드·메시지)은 무엇인가? → 기존 오류 응답 형식을 그대로 쓴다
  - 타 도메인 테이블·집계(정산·통계류)에 파급이 있는가? → DB 변경 없음
  - 기존 데이터 이관·백필이 필요한가? → 마이그레이션 없음
  - 변경 대상 화면이 전부 나열됐는가? (누락 화면 없음 확인) → 나열된 화면이 전부: UIS-ORD-001, UIS-ORD-002, UIS-ORD-003, UIS-ORD-004, UIS-ORD-005 장바구니
  - 빈 값·오류 상태의 화면 표기는 무엇인가? → 0건·오류·로딩 표시는 기존 규칙(UIS §5)을 그대로 따른다
  - 이 변경으로 생기거나 바뀌는 화면 상태 중 스토리(상태별 실물)로 남길 것은 무엇인가? → 바뀌거나 새로 생기는 상태마다 스토리를 남긴다(기존 스토리는 깨뜨리지 않는다)

### UIS-MBR-002 (로그인 화면 · 신규 UIS · 구현 모듈 shop-web)
- AS-IS: 없음 — 신규 스펙(예약 FUNC-member-004)
- TO-BE: 로그인 화면 — 위 SR-232 절의 요지·문답을 계약으로 신규 구현 (모듈 `shop-web` 안에)

### INF-MBR-003 (로그인 API · 신규 INF · 구현 모듈 shop-api)
- AS-IS: 없음 — 신규 스펙(예약 FUNC-member-005)
- TO-BE: 로그인 API — 위 SR-232 절의 요지·문답을 계약으로 신규 구현 (모듈 `shop-api` 안에)

### INF-MBR-004 (로그아웃·자동 로그인 토큰 API · 신규 INF · 구현 모듈 shop-api)
- AS-IS: 없음 — 신규 스펙(예약 FUNC-member-006)
- TO-BE: 로그아웃·자동 로그인 토큰 API — 위 SR-232 절의 요지·문답을 계약으로 신규 구현 (모듈 `shop-api` 안에)

## 제외(변경 없음 명시)
- (없음 — 영향분석에서 고른 스펙만 위에 있다)

## 회귀 범위
- 기존 동작(조회·정렬·표시 규칙)은 그대로 유지한다
- 기존 필드명·타입·의미는 그대로 둔다(추가만)
- 기존 오류 응답 형식을 그대로 쓴다
- 마이그레이션 없음
- 0건·오류·로딩 표시는 기존 규칙(UIS §5)을 그대로 따른다
- 바뀌거나 새로 생기는 상태마다 스토리를 남긴다(기존 스토리는 깨뜨리지 않는다)

## 롤백 요지
- 신규 스펙(UIS-MBR-002, INF-MBR-003, INF-MBR-004)의 코드·테이블을 역순으로 제거한다. 기존 스펙 변경 없음
