<!-- draft: tobe-adopt · 근거: 요구사항·확정 문답 9건·예약 스펙 0건·영향 스펙 INF-ORD-008, INF-ORD-009 -->
# SR-307 변경명세

> 기계 채택(zero-LLM): 요구사항 요지·확정 문답·예약 스펙을 **그대로** 옮겼다. 지어낸 사실은 없다 — `[미상]`은 사람이 채운다.
> 근거 파일: `docs/변경관리/SR-307/00_요구사항.md` · 확정 문답 9건

## AS-IS → TO-BE 비교
### SR-307 (요구사항 요지 — 전 스펙 공통)
- AS-IS: 현행 스펙은 아래 절의 파일이 말한다
- TO-BE (근거: 요구사항·확정 문답):
  - 앱이 서빙하는 쇼핑 화면(/shop)에서 상품 조회가 401로 막힌다. 브라우저는 API 키를 보내지 않는데 ApiKeyAuthFilter가 모든 /api/**에 키를 요구하기 때문이다(개발 서버는 프록시가 키를 넣어 줘서 가려져 있었다). 로그인 없이 볼 수 있어야 하는 **읽기 전용 상품 조회**만 공개 경로로 연다.
  - 대상: ApiKeyAuthFilter의 공개 경로에 GET 상품 목록·단건 조회 추가. GET 메서드일 때만 공개하고 다른 메서드는 지금처럼 막는다
  - 공개 대상은 판매중 상품 정보에 한정한다(회원·주문·장바구니·관리자 경로는 그대로 인증 필요)
  - 회원·주문·장바구니·CSV 등 기존 보호 경로의 인증 동작 불변(회귀 테스트로 확인)
  - 공개 응답에 회원 식별자·재고 원가 같은 내부 정보가 새로 들어가지 않는다(지금 응답 그대로)
  - 공개 경로라도 쓰기(POST/PATCH/DELETE)는 반드시 인증
  - 레이트리밋·캐시 정책은 이번 범위 밖
  - 이번 SR에서 하는 것과 명시적으로 빼는 것은 무엇인가? → 포함: ApiKeyAuthFilter의 공개 경로에 상품 조회 GET 2건(목록·단건)만 추가한다. 제외: 다른 경로의 인증 정책, 쓰기(POST/PATCH/DELETE) 공개, 레이트리밋·캐시·CORS 정책, 응답 필드 변경.
  - 반드시 유지돼야 하는 기존 동작(회귀 범위)은 무엇인가? → 특정 화면/API만: 회원·주문·장바구니·CSV·관리자 경로는 지금처럼 키 없으면 401 · 상품 조회도 GET 외 메서드는 401 · 키를 보낸 요청의 동작 불변 · 기존 Thymeleaf 화면과 개발 서버 프록시 경로 불변.
  - 기존 클라이언트와의 하위호환이 필요한가? → 필요 — 요청·응답 형식 변경 없음. 인증 요구만 GET 상품 조회에서 없어진다.
  - 오류 응답 계약(코드·메시지)은 무엇인가? → 새 오류 코드 없음. 공개된 GET은 200 또는 기존 404(없는 상품) · 공개 아닌 메서드·경로는 기존 401 그대로.
  - 타 도메인 테이블·집계(정산·통계류)에 파급이 있는가? → 없음 — 테이블·쿼리 변경 없음(인증 필터만 바뀐다).
  - 기존 데이터 이관·백필이 필요한가? → 불필요 — 스키마 변경 없음.
  - 변경 대상 화면이 전부 나열됐는가? (누락 화면 없음 확인) → 나열된 화면이 전부: 화면 변경 없음. 쇼핑 홈(UIS-ORD-008)이 앱 서빙 환경에서 상품을 받아오게 되는 효과만 있다.
  - 빈 값·오류 상태의 화면 표기는 무엇인가? → 해당 없음 — 화면 파일을 바꾸지 않는다. 쇼핑 홈의 빈 상태·오류 표기는 SR-302에서 이미 정의됨.
  - 이 변경으로 생기거나 바뀌는 화면 상태 중 스토리(상태별 실물)로 남길 것은 무엇인가? → 해당없음(스토리북 미사용) — 이 SR은 React 부품을 만들지 않는다.

### INF-ORD-008 (docs/05_설계서/order/INF/INF-ORD-008.md)
- AS-IS: 현행 스펙 `docs/05_설계서/order/INF/INF-ORD-008.md` — 모든 `/api/**`와 동일하게 `X-Api-Key` 없으면 401.
- TO-BE: 무키 GET 요청은 공개(200) — `ApiKeyAuthFilter`가 화이트리스트가 아니라 "무키일 때만" 인증 요구를 면제하는 예외로 처리(키를 보낸 요청은 종전 401/소유권 판정 그대로). GET 외 메서드는 여전히 무키 401. 쿼리·응답 스키마·오류 계약(404) 불변. 구현 반영 완료(SR-307.1, `docs/05_설계서/order/INF/INF-ORD-008.md`의 "인증" 절).

### INF-ORD-009 (docs/05_설계서/order/INF/INF-ORD-009.md)
- AS-IS: 현행 스펙 `docs/05_설계서/order/INF/INF-ORD-009.md` — 모든 `/api/**`와 동일하게 `X-Api-Key` 없으면 401.
- TO-BE: INF-ORD-008과 동일한 무키 GET 공개. 공개 판정은 `SKU-` 접두사 SKU 형식에 한정(`^/api/products/(SKU-[A-Za-z0-9-]+)$`) — 이 형식이 아닌 세그먼트(향후 서브리소스 등)는 매치되지 않아 무키 401(fail-closed)로 남는다. 단건은 `sale_yn` 필터가 원래 없어 판매종료 상품도 공개 대상(AS-IS 비즈니스 규칙 그대로, 이번 SR로 새로 생긴 노출 아님). 구현 반영 완료(SR-307.1, `docs/05_설계서/order/INF/INF-ORD-009.md`의 "인증" 절).

## 제외(변경 없음 명시)
- (없음 — 영향분석에서 고른 스펙만 위에 있다)

## 회귀 범위
- 특정 화면/API만: 회원·주문·장바구니·CSV·관리자 경로는 지금처럼 키 없으면 401 · 상품 조회도 GET 외 메서드는 401 · 키를 보낸 요청의 동작 불변 · 기존 Thymeleaf 화면과 개발 서버 프록시 경로 불변.
- 필요 — 요청·응답 형식 변경 없음. 인증 요구만 GET 상품 조회에서 없어진다.
- 새 오류 코드 없음. 공개된 GET은 200 또는 기존 404(없는 상품) · 공개 아닌 메서드·경로는 기존 401 그대로.
- 불필요 — 스키마 변경 없음.

## 롤백 요지
`modules/shop-api/src/main/java/com/sm/lab/shop/web/ApiKeyAuthFilter.java`의 `doFilterInternal`에서
`isPublicProductReadPath(path) && GET` 무키 예외 분기(1개)와 `PRODUCT_ITEM_PATH`/`PRODUCTS_LIST_PATH`
상수 2개를 제거하면 AS-IS(전 `/api/**` 무키 401)로 되돌아간다. 컨트롤러·서비스·DAO·DDL·응답 스키마는
변경하지 않았으므로 그 외 롤백 작업은 없다. `ApiKeyAuthIntegrationTest`의 SR-307 테스트 8건과
`ShopStaticResourceServingTest`의 감시 대상 교체(`/api/orders`)도 함께 되돌린다(후자를 원래대로
`/api/products`로 되돌리면 롤백 후 상태와 다시 일치한다).
