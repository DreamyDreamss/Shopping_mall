# LAB-104 변경명세

## 1. 신규 API — GET /api/orders/export (INF-ORD-015 예약)
- 파라미터: memberId(선택), **orderState(선택 — 정본, 목록 API와 동일 축)** + `status` 별칭
  (양쪽 동시엔 orderState 우선) — D11 정정. CSV 열 이름은 `status`(AC-3 확정 축 — D12).
- 처리: OrderService 목록 규칙 재사용(OrderDao.selectOrders — del_yn='N' 상시필터·최신순)
  → CSV 조립(BOM+헤더+RFC4180 인용) → attachment 응답. 새 조회 규칙 금지.
- 계약 상세: D10.

## 2. 변경 파일(예상)
- OrderController(+export 핸들러) · (필요시) OrderService 위임 메서드 · 테스트.
- DAO/mapper/스키마/화면: 무변경.

## 3. 추적
- linked_func: 재인덱싱 채번 FUNC(INF-ORD-015 예약분) 주석 필수.
